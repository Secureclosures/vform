// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'static.cdnlogo.com',
      '1000logos.net',
      'upload.wikimedia.org',
      'logos-world.net',
      'www.secureclosures.com',
    ],
  },
};

export default nextConfig;
